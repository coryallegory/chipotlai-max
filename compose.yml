services:
  chipotle-llm-provider:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      PORT: "3000"
      MAX_POOL_SIZE: "${MAX_POOL_SIZE:-5}"
    ports:
      - "3000:3000"
    restart: unless-stopped
